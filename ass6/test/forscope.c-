// C-F20
main() {
    int values[10];
    values[0] = 3;

    for (x in values) {
        int x;
        x = 1;
    }

    for (x in values) { {
            int x;
            x = 2;
        } }

    for (x in values) {
        int x;
        {
            int x;
            x = 3;
        } }

    for (x in values)
        if ( x>3) {
            int x;
            x=4;
        }

    for (x in values) {
        int x;
        if ( x>3) {
            int x; x=5;
        }
    }

    for (x in values)
        while (true)
            if ( x>3) {
                int x; x = 6;
            }
            else {
                int x; x = 7;
            }

    for (x in values)
        while (true)
            if ( x>3) 666;
            else {
                int x; x = 7;
            }
}
