// C-F20
char emu(char x, y, z);

main() {
    bool b;
    char c;
    int i;

    emu('z', 666, true);
    emu(c, i, b);
    emu(c+i, parrot, b & i);
    emu(c+i, emu(c, i, b), b & i);
    emu(c);
    emu(c, i);
    emu(c, i, b);
    emu(c, i, b, c+i);
    zot(c+i, b & i);
}
