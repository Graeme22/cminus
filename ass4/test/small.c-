// C-F20
//
bool a;
int b[100];

// procedure max
int max(int x, y)
{
    int z, zz;
    char c;

    if (x>y) z=x;
        else z=y;

    b[41] = max(b[42*1], 43);
    ;

    while (666>665 ) {
         int inn, four;            // syntactically & semantically OK

         aardvark[2] = inn;   // semantically this is illegal for many reasons
         b[3];
         inn[6];
         z = zz;
         break;
    }

    return;
}
