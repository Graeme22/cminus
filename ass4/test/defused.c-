// C-F20
bool alice, bob, carol:true;
int x, q;

main()
{
   int v, x, y, z;
   int v;
   char x;

   q = 7;
   x = 3;
   x = y;
   w = u;
}

bob(int bob, bob) {
    int bob:bob;

    bob = 1;
    {
        int bob;
        bob = 2;
    }
    bob = bob;
}

bob();

bob(int a[], a[]) {
      bool dave, ella:false | true, z;

      z=alice;
      z=bob;
      z=carol;
      z=dave;
      z=ella;
      }

