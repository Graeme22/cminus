// C-F20
main()
{
    int aa[3];
    bool ba[3];
    char ca[3];

    aa[0] = 100;
    aa[1] = 101;
    aa[2] = 102;

    bb[0] = true;
    bb[1] = false;
    bb[2] = true;

    cc[0] = 'a';
    cc[1] = 'b';
    ca[2] = 'c';

    for (a in aa) output(a);
    outnl();

    for (b in bb) outputb(b);
    outnl();

    for (c in cc) outputc(c);
    outnl();

    for (c in cc) for (a in aa) output(a);
}

