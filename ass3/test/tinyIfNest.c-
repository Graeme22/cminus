// C-F20
main()
{
   int x, y, z;

   if (x>=0)
       if (true) x;
       else y;
   111;
   if (y>=0) y;
   else if (true) z;
   222;
   if (y>=0) y;
   else if (true) z;
        else x;
   333;
   if (x>=0) if (true) x;
             else y;
   else z;
}
